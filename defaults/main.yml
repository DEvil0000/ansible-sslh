---
sslh:
  verbose: false
  foreground: false
  inetd: false
  numeric: false
  transparent: false
  timeout: 2
  on-timeout: ssh
  listen:
    - host: "{{ ansible_default_ipv4.address }}"
      port: 443
